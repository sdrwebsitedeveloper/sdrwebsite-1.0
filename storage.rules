rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 모든 사용자 읽기 가능
      allow read: if true;
      // 로그인된 사용자만 쓰기 가능
      allow write: if request.auth != null;
    }
  }
}